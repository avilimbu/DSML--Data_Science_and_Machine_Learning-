## Day 26 | Numpy introduction | Practice Qns

1. Use the numpy add function to add the values 34 and 29 in the cell below.

2. Declare a new array with contents [5,4,3,2,1] and slice it to select the last 3 items.

3. Select all the elements in the array below excluding the last one

4. Use arr as defined in 2.3. Exclude the last element from the list, but now only select
every 3rd element. Remember the third index indicates stride

5. You’ll need to combine array comparisons and logical operators to solve this one.
Find out the values in the following array that are greater than 3 AND less than 7.
The output should be a boolean array.